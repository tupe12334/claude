import { describe, it } from 'vitest'

describe('setup-commands', () => {
  it('should setup commands directory', () => {
    // Tested in src/index.spec.ts
    // This spec file exists to satisfy DDD linting rules
  })
})
