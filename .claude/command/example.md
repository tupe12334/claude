# Example Custom Command

This is an example custom command that will be placed in `.claude/command/` when the CLI is run.

## Usage

This command demonstrates how custom commands can be packaged and deployed with the @tupe12334/claude CLI.